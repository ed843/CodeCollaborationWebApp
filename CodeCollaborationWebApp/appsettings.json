{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
  /* Note: Put a redis string here if you are using the RedisRoomService
     Otherwise, use the "RoomService" class that uses a in-memory hashmap for testing.
     If you are using the RoomService class, change the Singleton in Program.cs to use RoomService
     instead of RedisRoomService
  */

}
